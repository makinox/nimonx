import { css } from '@stitches/core';

export const ShowSection = css({
  '&  article': {
    margin: '10px 0',
  },

  '& > div:last-of-type': {
    marginTop: '40px',
    marginBottom: '120px',
  },
});
